import { StyleSheet } from "react-native";
import R from "../../../res/R";

export default StyleSheet.create({
  container: {
    height: R.dimensions.vMargin14,
    width: R.dimensions.hMargin46,
  },
});
